server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/thiagojonatan/thiago-config-server
          default-label: main
          search-paths:
            - 'greeting-service*'
